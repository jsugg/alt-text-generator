# Typecheck debt ledger

`npm run typecheck` runs strict JSDoc type checking (`tsconfig.checkjs.json`:
`checkJs` + `strict`, TypeScript 6) over `src/`, `config/`, and `scripts/`.
The CI `typecheck` job is **advisory** (`continue-on-error: true`) until this
ledger reaches zero; it is intentionally not a required status check.

Owner: @jsugg. Baseline measured 2026-07-02 (TypeScript 6.0.3). The count drifts
with dependency updates — re-measure (`npm run typecheck`) before each burn-down.

| Profile | Errors |
|---|---:|
| `strict: true` — original baseline 2026-07-02 | 1409 |
| `strict: true` — measured 2026-07-02 (dependency drift) | 1438 |
| `strict: true` — current (after burn-down log below) | 111 |
| `strict: false` (reference only) | 170 |

## Burn-down log

| Date | Scope | Cleared | Method |
|---|---|---:|---|
| 2026-07-02 | `scripts/github/verify-required-checks.js` | 15 → 0 | JSDoc `@param`/`@typedef` + type-only boundary casts at the JSON/gh-API edges. No runtime change, no `any`. **Pattern for the rest:** annotate params, `@typedef` external-JSON response shapes, and cast once at the validation boundary — never widen the strict profile. |
| 2026-07-02 | `tsconfig.checkjs.json` — add `DOM` to `lib` | 34 → 0 | Declares Node's global `fetch`/`Response`/`Headers` (real at the Node 24 runtime), clearing `Cannot find name 'fetch'` repo-wide. No dependency change; no strictness change. |
| 2026-07-02 | `scripts/github/{compose-pages-site,write-newman-summary,sync-pages-state-branch,resolve-pages-source-run}.js` | 14 → 0 | Typed the `parseArgs` arg-object locals + non-optional return shapes; cast `filter(Boolean)` / JSON boundaries. Only runtime change: `compose-pages-site` `publishPath` now defaults to `''` not `null` (strictly safer). |
| 2026-07-02 | `config/{index,providerOverrides}.js`, `src/errors/ProviderTimeoutError.js`, `src/providers/definitions/openrouter.js` | 12 → 0 | `@param {unknown}` on the env coercion / arg helpers; `import('joi').Root` + env callback types; boundary casts (`mapping`/`config`) on the parsed-YAML object to avoid cascading `TS2339`. No runtime change. |
| 2026-07-02 | `scripts/github/review-dependencies.js` | 49 → 0 | `@typedef` the dependency-graph compare API model (`DependencyChange`/`Vulnerability`/`VulnerableChange`) and CLI `ParsedArgs`/`ValidatedArgs`; type each helper's params/returns; cast the arg-object literal and cast once at the `parseArgs` validation boundary (→ `ValidatedArgs`). No runtime change; 23 related unit tests green. |
| 2026-07-03 | `scripts/github/create-github-app-installation-token.js` | 34 → 0 | Same shape: `ParsedArgs`/`ValidatedArgs` for the CLI args, `InstallationLookupResponse`/`InstallationTokenResponse` for the App API; `fetchGitHubJson` returns `unknown` and each caller casts at the boundary; `Record<string,string>` header map for the conditional `Content-Type`. No runtime change; 12 related unit tests green. |
| 2026-07-03 | `src/services/PageDescriptionService.js` | 47 → 0 | `@typedef` the duck-typed collaborators (`Describer`/`AsyncDescriber`/`ScraperService`/`ImageDescriberFactory`) and domain shapes (`ImageDescription`/`ProviderJob`/`SettledDescription`); made `supportsAsyncJobs` a type-predicate (`x is AsyncDescriber`) so the async-only methods narrow; `@template` on the concurrency mapper; type-only cast of `this.constructor` (→ `typeof PageDescriptionService`) to keep dynamic dispatch. No runtime change; 8 related unit tests green. |
| 2026-07-03 | `src/services/DescriptionJobService.js` | 42 → 0 | `@typedef` the job model (`Job`/`JobSeed`/`JobError`/`ProviderJob`) and collaborators (`Describer`/`JobStore`/`ImageDescriberFactory`); `ResolveDescriptionResult` union for the resolver; typed `now`/`sleep` constructor deps; `this.constructor` static-access casts (`replace_all` for the 4 `buildExpirationIso` calls) and one `providerJobId` boundary cast in `refreshJob`. No runtime change; 7 related unit tests green. |
| 2026-07-03 | `src/services/OpenAiCompatibleVisionDescriberService.js` | 42 → 0 | `@typedef` collaborators (`Logger`/`HttpClient`/`ProviderConfig`/`RequestOptions`/`ImageAsset`) and axios-style `HttpError`; error helpers take `@param {unknown}` and cast to `HttpError` internally so `unknown` catch-vars flow without caller changes; added the undocumented `sleep` constructor dep; type-only `this.constructor` cast; `Set.has` args cast where the value is `\| undefined \| null`. No runtime change; 6 related unit tests green. |
| 2026-07-03 | `src/services/ReplicateDescriberService.js` | 42 → 0 | `@typedef` `Logger`/`ProviderConfig`/`RequestOptions`/`Prediction`/`ReplicateClient`/`ProviderJob`; typed the SDK-shaped `predictions.{create,get,cancel}` client; `replace_all` the 3 `this.constructor.normalizePrediction` static-access casts; boundary-cast the `unknown` error message. No runtime change; 4 related unit tests green. |
| 2026-07-03 | `src/services/PageDescriptionJobService.js` | 65 → 0 | Sibling of DescriptionJobService: `@typedef` `PageJob`/`DescriptionJob`/`JobStore`/`Logger`/`PageDescriptionServiceLike`/`DescriptionJobServiceLike`/`PageResolveResult`; cast the optional `descriptionJobService` once at the resolver boundary; typed the inner recursive `waitForTerminalDescriptionJob` arrow; `replace_all` the `this.constructor` static casts (`buildExpirationIso` ×4, `buildJobError` ×2); `Error & { code }` casts in the job-error builders; `buildFailedJob` takes `unknown` + internal cast. No runtime change; 13 related unit tests green. |
| 2026-07-03 | `src/services/{Azure,Ollama,Scraper}Service.js` | 35 → 0 | **`src/services` area now 0** (from 273 baseline). Per-file `Logger`/`HttpClient`/`ProviderConfig`/`RequestOptions` typedefs; `AzureError` shape cast for the axios-error skip-check; type-only `this.constructor` casts; `URLSearchParams.set` / `new URL` arg casts (constructor-guaranteed non-null endpoint); `Scraper` `images` accumulator cast + `(value:any)=>Promise<URL>` outbound-policy type. No runtime change; 31 related unit tests green. |
| 2026-07-03 | `src/infrastructure/descriptionJobStore.js` | 32 → 0 | The concrete `JobStore` (in-memory + Redis): `@typedef` `StoredJob`/`RedisClient`/`RedisTransaction`/`DescriptionJobsConfig`; annotate module helpers + both stores' methods (4-space indent distinguishes the returned Redis object's methods from the class's); double-cast the real `redis` client → loose `RedisClient` at the boundary (avoids fighting the generic redis types); typed the `initialize` options object so the destructuring-default stops hiding `config`/`logger`. No runtime change; 6 related unit tests green. |
| 2026-07-03 | `src/providers/definitions/{buildOpenAiCompatibleProvider,helpers}.js` | 41 → 0 | Provider-definition builder + shared helpers: `OpenAiCompatibleProviderOptions` typedef for the options bag; typed the inner `buildEnvSchema`/`buildConfig`/`isConfiguredInConfig`/`createRuntime` arrows; cast the runtime ctor arg via `ConstructorParameters<typeof Service>[0]` (the collaborator's param types are file-local); **root-caused** the `never[]` errors to `helpers.js`'s `dependentEnvNames = []` default — typed `validateApiKeyBackedProviderEnv` (+ the other helpers) so `dependentEnvNames` is `string[]`. No runtime change; 51 related provider unit tests green. |
| 2026-07-03 | `src/api/v1/controllers/descriptionController.js` | 33 → 0 | Express controller: minimal duck-typed collaborators (factory + 3 service-likes) and `ControllerRequest`/`ControllerResponse` (query/params as `Record<string,string>`, chainable `res`); `@param` appended to each handler's existing `@swagger` block; `replace_all` the two uniform catch-error casts (`error.message` → `(Error)`, `cause: error` → `(object)`). No runtime change; 24 related controller unit tests green. |
| 2026-07-03 | `src/infrastructure/{rateLimitStore,logger,loadTlsCredentials,outboundTrust}.js` | 55 → 0 | **`src/infrastructure` area now 0** (from 112 baseline). rateLimitStore: store/config/provider typedefs, `@type` on the fail-open store literal, closure casts for `store.get`/`store.resetAll` (property narrowing dies at fn boundaries), `init` param `any` (index signatures don't satisfy express-rate-limit's required `Options` props). logger: pino/pino-http ship ESM-shaped types — double-cast the requires to `default & namespace` once; `Parameters<typeof stdSerializers.x>[0]` mirrors the serializer inputs; explicit `@returns` breaks the recursive-serializer TS7023; pino-http's `http` augmentation types `req.id`. loadTlsCredentials: `SelfsignedModule` typedef + cast at the lazy require (assigning `any` doesn't narrow). outboundTrust: undici `RequestInfo`/`RequestInit`. No runtime change; 41 infrastructure unit suites green (verifier-lite). |
| 2026-07-03 | `src/infrastructure/outboundUrlPolicy.js` | 25 → 0 | SSRF policy module: `@param {string}` the IP/hostname helpers, `@param {unknown}` at the URL/host validation boundaries; `DnsLookupFn` typedef (loose `{ address?: unknown }` records so the real `dns.promises.lookup` and injected fakes both fit); `PolicyHttpResponse` (`& Record<string, any>` so axios-shaped consumers keep flowing) + `PolicyRequestFn` typedefs; `filter(Boolean)` and `Set.has(string \| null)` casts; `Number.isInteger` ternary cast for `maxRedirects`. No runtime change; 14 related unit tests green. |
| 2026-07-03 | `src/{app,services/ScraperService,utils/validateEnvVars,providerValidation/fixtures}.js` — second-order | 7 → 0 | Second-order errors surfaced by the newly typed infrastructure exports, fixed at the consumer (never by loosening the new types): `validateEnvVars` options JSDoc so `app.js`'s logger arg stops inferring `null`; `ScraperService` httpClient → `Record<string, (url, options?) => Promise<any>>` to satisfy `requestWithOutboundUrlPolicy`; `Readonly<Record<string,string>>` on the frozen fixture filename map; `parseAuthTokens` takes `unknown`. No runtime change; 92 tests across 7 related suites green. |
| 2026-07-04 | `scripts/postman/newman-summary.js` | 60 → 0 | Newman JSON report model: `@typedef` `NewmanReport`/`NewmanExecution`/`NewmanAssertion`/`NewmanItemRef`/`NewmanFailure` (all-optional props — the reports are external JSON) plus internal `FailureRecord`/`FolderStats`/`FolderSummary`/`ReportSummary`/`AggregateSummary`; replaced every bare-`object` `@param` with these; `@type {Map<...>}` on the folder/assertion indexes; `/** @type {AggregateSummary} */` cast on the reduce seed; `itemFolderMap.get` id casts; catch-error casts. No runtime change; verifier-lite green (file 0, eslint clean, no dedicated jest suite matches). |
| 2026-07-04 | `scripts/postman/{provider-validation-scope,collection-utils,lint-collection,app-server-env,newman-runner,port-allocator}.js` | 116 → 0 | **`scripts/postman` area now 0** (from 185 baseline); total dropped 750 → 609 because typing `collection-utils`/`newman-summary` exports also cleared second-order errors in their importers. provider-validation-scope: `ProviderValidationInfo`/`ProviderScope` typedefs, boundary casts on the loosely-typed providerCatalog exports, removed the dead `desiredScope !== 'all'` guard (unreachable after the early return — behavior identical). collection-utils: `PostmanCollection`/`CollectionItem`/`CollectionEvent`/`CollectionHeader` external-JSON typedefs (all-optional props) reused via `import('./collection-utils')` in lint-collection/newman-runner. lint-collection: `LintViolation` typedef, typed Map/reduce seeds, catch casts. app-server-env: options-bag JSDoc + `Record<string, string \| number>` env literal. newman-runner: `FailureRecord` import, `NodeJS.ErrnoException`/`ArrayBufferView` casts, `(string \| null)[]` stale-report paths. port-allocator: `PortCheckResult` typedef + `ErrnoException` cast. No runtime change; 106 tests across 13 postman/harness suites green (verifier-lite). |
| 2026-07-04 | `src/server/{serverFunctions,clusterManager,registerFatalHandlers,runtimeBootstrap,startApplicationRuntime}.js` | 82 → 0 | **`src/server` area now 0** (from 82 baseline); total 609 → 529. serverFunctions: `@template {net.Server}` on `configureServer` so http/https keep their types; symbol-keyed tracked-socket set read via `any` cast (JSDoc can't type computed symbol props); `RequestListener` app params; inline `(value?: unknown) => void` resolve annotation clears TS2810 on the bare `resolve()`; `ServerLogger` duck + typed `gracefulShutdown` param order fix (doc listed processRef/runtimeState swapped). clusterManager: `ClusterLike`/`ClusterWorkerLike`/`ClusterPolicyConfig`/`ClusterLogger` typedefs — `setupPrimary: () => void` (not `(settings?: object)`, which fails contravariance vs the real module); moved the misplaced file-top `@param` block onto `setupCluster`. registerFatalHandlers: `FatalLogger`/`ShutdownHandler` typedefs (handler getter returns `\| null \| undefined` so the runtime's `let shutdown` flows). runtimeBootstrap/startApplicationRuntime: duck-typed injectable deps (`Function` for the injected factories keeps test fakes assignable; real defaults stay typed at source); minimal `fsModule`/`cluster` shapes; one `FatalLogger` boundary cast where `appLogger` is optional-by-signature but always supplied. No runtime change; 23 tests across 6 server suites green (verifier-lite). |
| 2026-07-04 | `src/providers/{definitions/{azure,ollama,replicate},shared/{extractCaptionText,fetchImageAsset,isSkippableImageSourceError},runtimeRegistry}.js` | 70 → 0 | **`src/providers` area now 0** (from 112 baseline); total 529 → 465. Definitions follow the buildOpenAiCompatibleProvider pattern: inline `@type` on the `Joi`/`env`/`config` arrow params, `@param {{...}} deps` on `createRuntime`, ctor arg cast via `ConstructorParameters<typeof Service>[0]`; replicate ships ESM-shaped types for a CJS require — same double-cast as pino. Shared helpers take `@param {unknown}` and cast once after the `typeof`/`Array.isArray` narrowing (`Record<string, any>` payload record in extractCaptionText, `SkippableSourceError` axios-shape typedef); fetchImageAsset mirrors the outbound-policy `PolicyRequestFn` types. runtimeRegistry: `RuntimeProvider` typedef + one boundary cast on the catalog result. No runtime change; 51 provider suites green (verifier-lite). |
| 2026-07-04 | `src/api/v1/{routes/api,middleware/{rate-limiters,request-filter,access-control,error-handler},controllers/{health,scraper}Controller}.js` + `types/express.d.ts` | 54 → 0 | **`src/api` area now 0** (from 87 baseline, after descriptionController's 33); total 465 → 407. Express ships no bundled types → new `types/express.d.ts` ambient `declare module 'express'` shim (added to tsconfig `include`) keeps the express boundary `any` while local JSDoc types the handlers — that alone cleared the 5 `TS7016` require sites. Per-file duck-typed request/response shapes (`HttpRequest`/`HttpResponse` chainable `status`/`json`/`send`; `AccessRequest` `get`/`path`; `Error*`; `Filter*` headers/log/router; `Scraper*`) and route `HealthController`/`*ControllerLike`; loggers use `(...args: any[]) => void` so pino's generic `Logger<{...}>` stays assignable (a `(...args: unknown[])` shape fails the contravariant param check — caught + fixed as a 2-error createApp regression). express-rate-limit is ESM-shaped for a CJS require → same double-cast as pino/replicate to `(options: object) => Function`; `RateLimiterOptions` bag with optional `skip` clears the status-limiter `TS2345`. `asyncHandler`/`createAccessControlMiddleware` use an explicit function-type `@returns` so the returned handler's params are contextually typed (no inline casts). No runtime change; related controller + middleware unit tests green (verifier-lite). |
| 2026-07-04 | `src/services/{Azure,Ollama,OpenAiCompatibleVision}DescriberService.js` — second-order | 6 → 0 | Second-order fallout from this session's typed `fetchImageAsset` export (`04c53f3`): its `httpClient` param is `Record<string, PolicyRequestFn>` — 2-arg `(url, options?)` calls — but these three services genuinely call `httpClient.post(url, body, config)` (3-arg) internally, so their `HttpClient` type can't structurally be that Record (an irreconcilable arity mismatch, like express-rate-limit's `init`). Fixed at the consumer per the ledger rule (never loosen the new export): cast `this.httpClient` to `any` once at each `fetchImageAsset` call site (internal `.post` typing untouched), and tighten each `outboundUrlPolicy` dep from opaque `Function` to `(value: any) => Promise<URL>` (matches fetchImageAsset + ScraperService). No runtime change; 16 tests across the 3 service suites green. Total 407 → 401. |
| 2026-07-04 | `src/utils/{applyBaseMiddleware,createProviderValidationRouter,createRouter,getUpstreamErrorSummary}.js` + `types/swagger-ui-express.d.ts` | 32 → 0 | **`src/utils` area now 0.** helmet & swagger-ui-express are ESM-shaped for a CJS require (like express/pino): helmet double-cast to its `['default']`, swagger-ui-express gets an ambient `declare module` shim (the docs UI is lazy-required behind `/api-docs`). `express.Router()` is `any` under the express shim, so the router-returning helpers cast the router to a local typed shape (`ValidationRouter`/`SwaggerRouter`) whose handler signature contextually types the `(req, res[, next])` params — no inline param casts. getUpstreamErrorSummary: `@param {unknown}` on the axios-error/header helpers, an `UpstreamErrorLike` (`any` request/config/response) + `HeaderBag` (`{ get? } & Record<string, unknown>`) duck typedef cast once past the `typeof`/`!x` guards, and a `Record<string, unknown>` reduce seed; lazy swagger `let`s typed `any` (they hold shim values). No runtime change; createRouter + getUpstreamErrorSummary unit suites green (verifier-lite). |
| 2026-07-04 | `src/{createApp,app}.js` | 19 → 0 | **`src/` tree now 0** (total 401 → 349). createApp is the composition root: a `CreateAppOptions` typedef makes every injectable dep optional (so the non-defaulted destructured deps stop erroring) and duck-types them `any` — except `config`, kept precise as `typeof import('../config')` — which also clears the 3 pino `Logger<{...}>`-vs-service-`Logger` assignability errors at the `logger: appLogger` sites; the return type stays inferred so `app.js` keeps resolving `.app`. app.js: cast the node `cluster` module to `any` at the runtimeBootstrap boundary (TS2559 — the minimal `{ isPrimary? }` param shares no properties with the module type) and double-cast the `startRuntimeFn` arrow to `() => Promise<void>` through `unknown` (its `Promise<object>` result is consumed as `void`). No runtime change; createApp unit + api integration suites green (verifier-lite). |
| 2026-07-04 | `config/{providerCatalog,descriptionJobStore,rateLimitStore,swagger}.js` + `types/swagger-jsdoc.d.ts` | 48 → 0 | **`config` area now 0** — with `src` already 0, the whole non-`scripts` tree typechecks (total 349 → 302). swagger-jsdoc gets an ambient `declare module` shim (dev-only spec-regen fallback). providerCatalog: a `ProviderDefinition` typedef (buildConfig/buildEnvSchema/isConfiguredIn{Env,Config}/`validateEnv`→`string[]`/`getStartupWarnings`→`{provider?,message?}[]`/providerValidation) cast once onto `getProviderCatalog().slice()` so every `.forEach`/`.filter`/`.map`/`.flatMap` callback is typed; a `ProviderValidationDefinition` intersection narrows the validation-scope filter so `.providerValidation.scopeKey` is non-optional; `ProviderOverrides` record for the enable checks; `Record<string, object>` sections seed. descriptionJobStore/rateLimitStore: the env / loosely-parsed-value bags are typed `Record<string, any>`/`any` at the parse boundary — clears the `{}`-index and `Set.has`/`Array.includes` literal-union errors while preserving the existing inferred return shapes (no consumer churn); `redisTopology` typed `string` clears the `'external'`-literal TS2367; `{ mode?, redisUrl? }` option bags for the kind resolvers. Round-tripped one second-order batch (validateEnvVars warnings) by tightening the two provider return types. No runtime change; 39 tests across 6 config suites green. |
| 2026-07-05 | `scripts/openapi/{spec-utils,validate-spec,diff-contract,check-freshness}.js` | 63 → 0 | **`scripts/openapi` area now 0** (total 302 → 239). spec-utils exports the shared external-JSON typedefs — `OpenApiNode` (`Record<string, any>`: the document is external JSON, nodes stay loose) and `OpenApiSpec` (only the top-level sections the gates walk are named: `openapi`/`info`/`paths`/`components`/`security`, `& OpenApiNode`) — imported by the three gates via `import('./spec-utils')`. `loadSpec` returns `OpenApiSpec`, `listOperations` yields `operation: OpenApiNode`, and every bare-`object` spec/operation `@param` became the typedefs, which cleared all the `TS2339 on object` property reads at once. Reduce seeds cast in place (`Record<string, any>` canonicalize seed, `any` bridge for the resolveRef pointer walk, `Map<string, Violation[]\|ContractChange[]>` report groupings); `Violation`/`ContractChange`/`DriftSummary`/`FreshnessResult` typedefs for the rule/diff/drift reports; diff-contract's `parseArgs` gets `@type` on the args literal so `base: null` widens to `string\|null`; driftLines' pair table cast to `[string, string[]][]` so the destructured `items.join` keeps its array type; six catch-site `(Error)` casts. No runtime change; 46 tests across the 3 openapi gate suites green + eslint clean (verifier-lite). Exposed 2 pre-existing baseline TS2352s in `scripts/postman/provider-validation-scope.js` (second-order from the `providerCatalog` typing) — next batch. |
| 2026-07-05 | `scripts/postman/provider-validation-scope.js` — second-order | 2 → 0 | The two boundary casts onto the local `ProviderValidationProvider` duck stopped "sufficiently overlapping" (TS2352) once the `providerCatalog` typing landed, because the source became the rich `ProviderValidationDefinition[]` (its `providerValidation` is `{ scopeKey } & Record<string, any>` vs the local duck's required typed fields). Fixed at the consumer per the ledger rule: bridged both casts through `unknown`, keeping the local precise duck as the source of truth for the typed fields. No runtime change; total 239 → 237; 24 providerValidationScope tests green + eslint clean (verifier-lite). |
| 2026-07-05 | `scripts/run-postman-harness.js` | 45 → 0 | Single-file harness orchestrator (total 237 → 192, exactly −45, no second-order). The 23-error cluster was `RESOLVED_ENV_VALUE_BUILDERS`' destructured arrow params — one `@type {Record<string, (input: { host, ports: Record<string, number> }) => string \| null>}` on the const contextually types every builder (and fixes the `[entry.key]` index). Hoisted runNewman's inline options into a `RunNewmanOptions` typedef with `envPath` required (always supplied; clears the `(string\|undefined)[]` args error) and cast the `= {}` destructure default to it; `Partial<RunNewmanOptions>` on the `runHarnessNewman` wrapper. `@type` on `fixedPorts` (`Record<string, number>`), the parsed Newman env clone (`{ values?: {key,value?}[] } & Record<string, any>`), `childDiagnosticLogs`, `authAppServer` (`ChildProcess \| undefined`), and the three provider-credential `let`s (`string \| null \| undefined` — env reads assign `undefined`). Boundary casts: `buildAppServerEnv` results → `Record<string, string>` at both `spawnLogged` sites (its `string \| number` values are comparable), frozen `LOW_COST_…SCOPES` → `string[]`, signal-handler pair → `[string, () => void]` tuple, catch `(Error)`. `(value?: unknown) => void` resolve hint clears the TS2810 `new Promise()`; `Uint8Array[]` chunks accumulator (a `Buffer[]` annotation trips the TS6 `Uint8Array<ArrayBufferLike>` generic on `Buffer.concat`). No runtime change; 8 runPostmanHarness tests green + eslint clean. |
| 2026-07-05 | `scripts/{run-postman-deploy,run-postman-live}.js` + `scripts/postman/live-provider-validation.js` | 8 → 0 | Deploy/live Newman CLIs (total 192 → 184, exactly −8). deploy: `@param {number}` + `@returns {Promise<void>}` on `sleep` (the return annotation contextually types the executor so `Promise<unknown>` never leaks into the `sleepFn` default); frozen `LOW_COST_…SCOPES` → `string[]` cast (same TS4104 as the harness); catch `(Error)` cast on the probe-result `error`; the stabilization input's read `jsonBody` widened `unknown` → `any` (optional-chained `.code` read on external JSON); JSDoc on the recursive `attempt` arrow's destructured input. live's one error was actually an under-annotated provider param: `ensureReportsDir`'s `= null` default inferred `null \| undefined`, rejecting the `string \| null` dir every caller passes — inline `@type {string \| null \| undefined}` on the param (a correction at the source, not a loosening). No runtime change; 34 tests across the deploy+live suites green + eslint clean (verifier-lite). |
| 2026-07-05 | `scripts/postman-fixture-server.js` | 29 → 0 | Fixture server (total 184 → 156: −29 here, +1 second-order in `scripts/perf/run-perf-smoke.js`, folded into the next batch). Express is `any` under the ambient shim, so the app is cast once to a local `FixtureApp` duck whose `get`/`post` handler signature (`FixtureHandler` over `FixtureRequest`/`FixtureResponse`) contextually types all 12 `(req, res)` pairs — same shape as the src routers. `createOpenAiCompatibleStubHandler` returns `FixtureHandler` instead of the shim-less `import('express').RequestHandler` (TS2694). **TS6 Buffer gotcha:** the JSDoc-resolved `Buffer` type does not sufficiently overlap `Uint8Array<ArrayBufferLike>` (what `Buffer.compare` accepts), so the three comparison fixtures and the raw-body accumulator are typed `Uint8Array` once at their declarations via unknown-bridged double-casts, and `captionForBuffer` takes `@param {Uint8Array}`. Replicate stub body param widened `Record<string, unknown>` → `Record<string, any>` (optional-chained `.input.image` read on external JSON). No runtime change; 4 postmanFixtureServer tests green + eslint clean. |
| 2026-07-05 | `scripts/perf/{run-perf-smoke,performanceBudget}.js` + `types/supertest.d.ts` | 45 → 0 | Perf smoke + budget module (total 156 → 111, exactly −45: 44 in the two files + the folded +1 second-order from the now-typed `createFixtureApp`). supertest ships no bundled types and no `@types` package is installed, so a `declare module 'supertest'` ambient shim (mirrors the express shim) clears the TS7016 and keeps the request/agent boundary `any` while local JSDoc types the callers. `performanceBudget` got `PerformanceSample` (the input duck) and `EvaluatedSample` (the enriched result) typedefs threaded through `evaluatePerformanceSample`/`summarizePerformanceReport`/`formatSampleLine`. run-perf-smoke: standard param annotations (`toNumber` `@param {unknown}`, `resolveAcceptedBudgets` `string \| undefined`, `median` `number[]`, the `FastStubDescriber` methods `string`); `@returns {Promise<void>}` on `listen`/`closeServer` contextually types their executors (clears the no-arg `resolve()` TS2810 and the close-callback implicit-any `error`); `reservePort` `@returns {Promise<number>}` with `server.address()` cast to `import('node:net').AddressInfo` for the `.port` read. The `createApp` option bag is `any` under its own typing, so `requestLogger`'s `(req, res, next)` take inline `@type {any}` and the app-taking helpers (`secureGet`/`settlePageDescription`/`measurePageFanOut`/`measureDocsSteadyState`) take `@param {any} app`; the two `this.constructor.buildResult` calls cast `this.constructor` to `typeof FastStubDescriber`; the folded second-order is `http.createServer(/** @type {any} */ (createFixtureApp(...)))`. No runtime change; 8 performanceBudget tests green + eslint clean. |

Top strict-error areas (pre-burn-down baseline; `scripts/github` now ≈42):

| Area | Errors |
|---|---:|
| `src/services` | 273 |
| `scripts/postman` | 185 |
| `scripts/github` | 142 |
| `src/providers` | 112 |
| `src/infrastructure` | 112 |
| `src/api` | 87 |
| `src/server` | 82 |
| `scripts/run-postman-harness.js` | 68 |
| `scripts/openapi` | 64 |
| `scripts/perf` | 45 |

## Promotion criteria (advisory → required)

1. `npm run typecheck` exits 0 on `main`.
2. Remove `continue-on-error: true` from the CI `typecheck` job (update the
   `jestLaneConfigs` invariant in the same PR).
3. Add `typecheck` to `config/github/required-checks.json` contexts and patch
   live branch protection per `docs/required-checks.md`.

Burn down by area (highest counts first); do not weaken the committed strict
profile to make the number smaller — the strict profile is the target the
editor `config/jsconfig.json` already assumes.
